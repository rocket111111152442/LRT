node_modules
.next
out
build

# Keep environment variables out of version control
.env
.env.*
!.env.example
.env*.local

/generated/prisma

# Dumps de migration de données (ne pas versionner)
backups

tsconfig.tsbuildinfo

# Projet ASSURIO independant (repo git separe)
assurio/
.claude/

# Certificats locaux du serveur interne (generes sur chaque PC)
serveur-interne/*.pem
